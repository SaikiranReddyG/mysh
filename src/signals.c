#include "shell.h"

void setup_signals(void)
{
    /* TODO: Set up signal handlers (SIGINT, SIGTSTP, SIGCHLD) */
}

void signal_handler(int sig)
{
    (void)sig; /* Parameter unused in stub */
    /* TODO: Handle signals */
}
